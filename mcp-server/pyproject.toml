[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dory-mcp"
version = "0.1.0"
description = "Dory MCP server for AI agent spend tracking"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]
