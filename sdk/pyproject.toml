[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "keel-sdk"
version = "0.1.0"
description = "Spend tracking for Anthropic AI agents"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.30.0",
]
